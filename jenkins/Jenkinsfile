pipeline {
    agent any

    triggers{
        githubPush()
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building in ...'
                pwd
                sh './gradlew clean build'
            }
        }
    }
}
